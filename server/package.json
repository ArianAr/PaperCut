{
  "name": "@papercut/server",
  "version": "0.1.0",
  "private": true,
  "description": "PaperCut Next.js server — API and interactive log canvas",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "echo \"Server scaffold lands in a follow-up PR\" && exit 1",
    "build": "echo \"Server scaffold lands in a follow-up PR\" && exit 1",
    "start": "echo \"Server scaffold lands in a follow-up PR\" && exit 1",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No lint yet\" && exit 0",
    "typecheck": "echo \"No typecheck yet\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
